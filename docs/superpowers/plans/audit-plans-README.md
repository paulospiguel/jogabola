# Implementation Plans

Generated by the improve skill on 2026-07-11 at commit `d57f784` (branch `releases/jogabola-teams/release02`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Verification baseline — vitest, CI, first tests | P1 | M | — | DONE |
| 002  | Auth/authz on payment, attendance, chat-token surfaces | P1 | M | 001 | DONE |
| 003  | Guest OTP rate limiting + lockout | P2 | M | 001 (soft), after 002 | DONE |
| 004  | Finish PostHog→Statsig migration + dep cleanup | P2 | M | — (coordinate merges with 002) | IN PROGRESS |
| 005  | Replace stale agent docs + add README | P2 | S | — | TODO |
| 006  | Fix unconfirmed destructive actions + dead-clicks + ambiguous tags in `/timer` | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 depends on 001**: the auth fixes are only trustworthy with the test harness and mock pattern 001 establishes; 002's Step 7 tests are written in that pattern.
- **003 after 002**: both edit `src/actions/guest-rsvp.actions.ts`; 002 changes `verifyGuestOTP`'s return shape (adds guest access token), which 003 must not clobber.
- **004 vs 002**: 004 changes only the `trackServerEvent` import line in the same 6 action files 002 edits — trivial merge, but land sequentially, not in parallel branches.
- **005 anytime**: only note — its README analytics wording depends on whether 004 landed (handled inside the plan).

## Audit summary (2026-07-11, standard depth)

Full workflow: recon + 4 parallel category audits (correctness/security, perf/tests, debt/deps, dx/docs/direction), all table findings re-verified against source by the advisor. Not audited in depth: the timer module internals, email template components, i18n locale completeness, Remotion usage.

Findings planned: unauthenticated/unauthorized server actions (→002), missing manager checks (→002), Ably token over-scoping (→002), zero-test baseline (→001), OTP brute-force (→003), incomplete analytics migration + dead deps + nodemailer/drizzle advisories + lockfile duplication (→004), stale CLAUDE.md/GEMINI.md/posthog report + missing README (→005).

Findings NOT planned (still open, lower leverage):
- N+1 queries in cron reminders (`src/lib/cron-reminders.ts:80-131, 167-252`) — real, S/M effort, batch with `inArray` + Set membership; fine to do opportunistically.
- CORS wildcard `Access-Control-Allow-Origin: *` (`next.config.ts:66-76`) — restrict to app origins; S effort.
- Mock fixtures rendered as real data in arena rankings/historical (`src/app/(mobile)/(protected)/arena/{rankings,historical}/_fixtures/`) — needs a product decision (build real queries vs. gate the screens) before a plan is honest.
- Unoptimized PNGs in `src/assets/images/` (160–536 KB each) — convert to WebP + `next/image` sizing.
- TS enums vs CONTEXT.md convention (`src/lib/utils.ts:34`, `src/components/ui/slider.tsx:8`) — trivial, batch into any nearby change.
- God file `payments.actions.ts` (717 lines) — split only after 001/002 land (tests first).

Direction options (maintainer decisions, not planned):
- Fines system half-built: schema + auto-creation live, `FEATURES.fines=false`, zero UI — fines silently accumulate in DB today; either build captain UI or stop writing rows.
- AI payment-proof validation: promised in CONTEXT.md ("ou via IA"), `paymentPrechecks` plumbing exists client-side only; server-side vision inference would also properly fix the `verifyPaymentProof` trust issue.
- Timer → team ecosystem bridge: standalone `/timer` captures goals/cards/results but never feeds `matchSessions`/stats.
- Team balancer: algorithm + self-assessment UI built, gated "Fase 2" on ratings adoption.

## Findings considered and rejected

- **".env committed to repository"**: false — `.env` was never tracked (`.gitignore` covers `.env*`; zero git history). Local-only file. `.env.example` contains placeholders only.
- **Cron routes unauthenticated**: false — both cron routes gate on `isAuthorizedCron(req)` (`src/lib/cron-auth.ts`).
- **OTP timing attack via SQL equality**: negligible over network latency; moot once plan 003's lockout lands (constant-time compare included there anyway).
- **Ably token allows publish**: overstated — capability is `["subscribe", "history"]` only; the real issue (wildcard channel scope) is in plan 002.
- **Consolidate gsap/lottie-react/framer-motion**: rejected — each has distinct, real usage (DOM inertia, Lottie JSON playback, React transitions); consolidation cost exceeds bundle benefit. `motion` (the duplicate of framer-motion) IS dead and removed in 004.
- **`verifyPaymentProof` auto-approves payments**: overstated — it sets `paid_unverified`, which captains still see as "VALIDATING"; the unauthorized-mutation part is fixed in 002.
- **Email send failures silently swallowed in RSVP**: intentional non-blocking pattern (commented in code); acceptable.
- **`updatePaymentStatus` co-captain scope too broad**: team-level authorization appears to be the intended model (`canManageTeam` doc comment says exactly this); policy question, not a bug.

## Plan 006 addenda (2026-07-13, manual QA on `/timer`)

- **"Floating avatar overlaps 'Registar golo' button on mobile"**: false — that's the Next.js dev-mode indicator, not app UI; won't render in production. See plan 006 corrections section.
- **"Setup wizard step 2 sometimes skips the team-editor fields"**: not reproduced on source review; `setup-drawer.tsx` has no conditional bug. Downgraded to a P2 animation-timing verification task (plan 006, Step 7) rather than the P0 data-loss bug it first looked like.
- **Direction option added**: Treino vs Jogo share one data model/UI end-to-end (`match.type` only picks an emoji in the hub list) — flagged in plan 006 Step 6 for a maintainer decision, not implemented.

## Plan 006 execution notes (2026-07-13, commit `advisor/006-timer-cronometro-ux-fixes`)

- **Step 1 (DONE)**: `confirmEnd` state added to `MatchControls`. Both the last-period skip/Square button and the "Terminar jogo agora" link share one arm-and-confirm flow via `armEnd`/`commitEnd`/`cancelEnd` helpers, identical pattern to the existing `confirmRestart`. `onBlur` auto-reverts the armed state.
- **Step 2 (DONE)**: `log-goal-sheet.tsx` and `log-card-sheet.tsx` now initialize their selection state to the sole player's id when `team.players.length === 1`, eliminating the dead-click on the confirm button.
- **Step 3 (DONE)**: `event-timeline.tsx` team tag changed from `slice(0, 3)` to `slice(0, 1)` — single-letter initial consistent with the avatar convention used throughout the component tree; "Equipa A"/"Equipa B" now render as "E" (differentiated by color swatch) instead of the ambiguous "EQU"/"EQU".
- **Step 4 (DONE)**: `match-view.tsx` now renders a distinct "Jogo terminado" banner (Flag icon + "Ver resumo" button) above the disabled controls when `match.state.status === "ended"` and the summary modal is closed. The `useEffect` that auto-opens the summary on first end was already present; the banner gives the screen a clear identity after the user dismisses the summary.
- **Step 5 (DONE)**: `event-timeline.tsx` delete button changed from `opacity-0 group-hover:opacity-100` to `opacity-40` always-visible with `hover:opacity-100 active:opacity-100 focus-visible:opacity-100`. Control is now discoverable on touch without requiring hover.
- **Step 6 (flagged, no implementation)**: Treino vs Jogo product decision left open for maintainer. Options: (a) keep as relabeled Jogo, adjust copy; (b) give Treino its own flow (single roster, drill/segment tracking). Tracked above in "Direction options".
- **Step 7 (closed, not reproduced)**: Setup wizard flicker not reproducible on source review. `setup-drawer.tsx` renders step content with plain `useState` — no conditional bug found. Closed as a one-off render glitch, likely `AnimatePresence mode="wait"` exit/enter overlap.

