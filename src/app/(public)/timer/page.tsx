import { TimerContent } from "@/features/timer/components/timer-content";

export const metadata = {
  title: "Timer | JogaBola",
  description: "Gerencie suas partidas em direto",
};

export default function TimerPage() {
  return <TimerContent />;
}
